import {
  InnerContainer,
  LeftContainer,
  RightContainer,
  Wrapper,
} from "./StyledFooter";

const Footer = () => {
  return (
    <Wrapper>
      <InnerContainer>
        <LeftContainer></LeftContainer>
        <RightContainer></RightContainer>
      </InnerContainer>
    </Wrapper>
  );
};

export default Footer;
